CREATE TABLE company_settings(

id SERIAL PRIMARY KEY,

business_name VARCHAR(200),

kra_pin VARCHAR(50),

vat_number VARCHAR(50),

branch_number VARCHAR(50),

etims_device_id VARCHAR(100),

etims_environment VARCHAR(20)

);


ALTER TABLE company_settings

ADD COLUMN etims_serial_number VARCHAR(100),

ADD COLUMN etims_branch_id VARCHAR(100),

ADD COLUMN currency VARCHAR(10) DEFAULT 'KES',

ADD COLUMN api_key TEXT,

ADD COLUMN api_secret TEXT;